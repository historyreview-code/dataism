# Dataism 项目评审报告

> 评审范围：dataism-core 引擎 + dataism-exhibit「时辰」+ dataism-17 + dronestorm  
> 评审维度：技术架构、视觉审美、产品逻辑、工程化成熟度

---

## 一、总体印象

Dataism 是一个架构清晰、审美在线、概念完整的数字艺术项目。核心亮点包括：

- **引擎-作品解耦**：`dataism-core` 作为可复用的粒子/音频/章节引擎，让衍生作品边际成本极低
- **十二时辰叙事**：用中国传统时间系统作为策展骨架，概念高级且有文化辨识度
- **确定性渲染**：mulberry32 种子锁定 + git tag 版本锚定，"版画编号"式的产品化思路极为聪明
- **GPU 侧主题过渡**：50k 粒子不重建几何体，uniform lerp 实现零成本换章

**当前阶段判断**：从「作品」到「展品」的 v1.1 版本已经完成，但从「展品」到「商品」的 L3/L4 阶段仍有显著缺口。

---

## 二、技术层面改进建议

### 🔴 优先级 0（影响产品化出货）

#### 1. WebGL 上下文丢失恢复
**现状**：没有任何 `webglcontextlost` / `webglcontextrestored` 处理。  
**风险**：展厅 7×24 小时运行，GPU 驱动更新、电源管理、过热降频都可能导致上下文丢失，直接黑屏。  
**建议**：
```js
// 在 Canvas onCreated 中注册
const onContextLost = (e) => { e.preventDefault(); /* 显示"恢复中"遮罩 */ };
const onContextRestored = () => { /* 重建所有 BufferGeometry + ShaderMaterial */ };
gl.domElement.addEventListener('webglcontextlost', onContextLost);
gl.domElement.addEventListener('webglcontextrestored', onContextRestored);
```
**核心难点**：`ParticleCloud` 中 `useMemo` 生成的 geometry 在恢复后需要显式重建，而 React 不会自动重跑 useMemo。需要把几何数据（positions/seeds/sizes）提升到 ref 或外部 store，恢复时调用重建函数。

#### 2. 性能自适应（粒子数 LOD）
**现状**：`COUNT = 50000` 是硬编码，没有考虑设备 GPU 能力分级。  
**风险**：M1 Mac mini 没问题，但 N100 / 旧 iPad / 某些展厅的嵌入式 Android 大屏可能直接掉帧到 <15fps。  
**建议**：
```js
function detectParticleCount() {
  const gl = document.createElement('canvas').getContext('webgl2');
  if (!gl) return 15000; // 降级
  const renderer = gl.getParameter(gl.RENDERER);
  const vendor = gl.getParameter(gl.VENDOR);
  // Apple Silicon / 中高端独显 → 50000
  // Intel 核显 / Mali GPU → 25000
  // 低端 / 未知 → 15000
  return tier;
}
```
将 `COUNT` 改为运行时可配置，并在 `PRODUCTIZATION.md` 的 L4（iPad/数字相框）中明确降配规格。

#### 3. DroneStorm 的 O(n×m) 粒子采样性能炸弹
**现状**：`particlesFromWind` 中，对每个粒子做线性扫描权重数组（256 个网格点），50k × 256 = **1280 万次操作**，在主线程同步执行。  
**风险**：每次 NOAA 数据更新时，UI 冻结 200-500ms（取决于 CPU）。  
**建议**：
- 预计算权重累积分布（CDF），然后用二分查找或 `O(1)` 逆变换采样
- 或者改用 alias method（Vose 算法），实现严格 `O(1)` 每粒子采样
- 更好：在 Web Worker 中做数据 → particle buffer 的转换，不阻塞主线程

#### 4. 自适应噪声门（展厅刚需）
**现状**：`useMicInput` 中 `BEAT_THRESHOLD = 1.5` 是固定值。  
**风险**：安静画廊和嘈杂咖啡馆的底噪差异巨大，固定阈值要么不响应，要么持续误触发。  
**建议**（已在 PRODUCTIZATION.md 中提到但未实现）：
```js
// 开机后前 10 秒采集环境底噪
const calibrationWindow = [];
// 运行中维护 running median of noise floor
// beat threshold = median + 2×MAD（中位数绝对偏差）
```

---

### 🟡 优先级 1（显著影响品质感）

#### 5. 颜色空间：sRGB lerp → Oklab/LCh lerp
**现状**：`THREE.Color.lerp` 在 sRGB 空间插值，中间态会发灰、发脏（尤其是蓝↔橙这种互补色过渡）。  
**影响**：十二时辰换章时的颜色呼吸不够"通透"。  
**建议**：使用 `three-stdlib` 的 `Color` Oklab 转换，或在 shader 中把颜色转到线性空间再 lerp。

#### 6. 粒子形状从圆点升级为"数据晶格"
**现状**：所有粒子都是 `gl_PointCoord` 软圆点，视觉语言单一。  
**建议**：根据章节主题更换粒子形状：
- 子时/丑时（夜章）：柔光圆点（现状）
- 巳时/申时（Storm）：六边形晶格（暗示数据网格）
- 未时（SalmonRun）：鱼形/水滴形
- 辰时/午时（Pulse）：十字星芒

实现方式：在 fragment shader 中根据 `vSeed` 的离散区间选择不同 SDF 形状，或上传一个 sprite atlas。

#### 7. 后处理管线：加入 Bloom + Vignette
**现状**：没有任何后处理，粒子高光区域无法"溢出"到相邻像素，缺乏"辉光感"。  
**建议**：使用 `@react-three/postprocessing` 添加：
- **Bloom**：让 core 高光区域真正"亮起来"，夜章的 dotGain 提升才有意义
- **Vignette**：轻微暗角，聚焦视线到椭圆云中心
- **Chromatic Aberration**：极低强度（0.003），增加数字/光学装置的质感

注意：Bloom 在 `AdditiveBlending` 粒子上效果极好，但需要把 `cloud.frag` 的 alpha 输出调高以让 N8A 捕获足够亮度。

#### 8. 字体加载兜底与 FOUT 处理
**现状**：`Songti SC` / `Noto Serif SC` 是系统字体，但如果运行在 Linux 展厅主机（N100）上，这些字体可能不存在，fallback 到衬线体会破坏宋体的优雅感。  
**建议**：
- 在 core package 中内嵌 `Noto Serif SC` 的 WOFF2 子集（只包含子丑寅卯等用到的字符，体积 <50KB）
- 或者使用 `font-display: swap` + 预加载，避免加载期间的闪烁

---

### 🟢 优先级 2（锦上添花）

#### 9. TypeScript 迁移
**现状**：纯 JS 项目，没有类型安全。  
**影响**：随着引擎复杂度增加（theme 的 behavior 参数已从 4 个扩展到 8 个），theme 类型不匹配的风险增大。  
**建议**：先给 `dataism-core` 添加 JSDoc 类型注解（零成本迁移），新代码用 `.ts`/`.tsx`，逐步重构旧文件。

#### 10. 减少全局污染
**现状**：`window.__shichenRaf`, `window.__shichenDraw`, `window.__shichenUni` 等调试用具挂在全局。  
**建议**：用 `const DEBUG = import.meta.env.DEV` 包裹，生产构建时 tree-shake 掉。

#### 11. `preserveDrawingBuffer` 性能成本
**现状**：`gl={{ preserveDrawingBuffer: true }}` 为 `?export=1` 调试通道而开。  
**建议**：仅在 `?export=1` 时开启，正常模式关闭（可减少 5-10% GPU 内存带宽占用）。

---

## 三、审美层面改进建议

### 1. 构图：竖屏版本
**现状**：椭圆云 `x=3.2, y=0.9` 是横屏构图，竖屏（65% 潜在点位）上粒子被极度压缩。  
**建议**：
- 检测 `aspect < 1` 时，切换为纵向椭圆（`x=1.2, y=2.8`）或旋转 90° 的粒子场
- 或者开发一种"竖屏特供"构图：粒子从顶部瀑布式流下，底部汇聚成涡旋（呼应"时间如水流"的意象）

### 2. 章节切换的"转场仪式"
**现状**：换章时只有标题卡淡入淡出，粒子颜色/行为在 ~12s 内渐变。  
**建议**：增加一个 2-3 秒的"转场仪式"——
- 全屏闪白/闪章节色（极低强度，如 0.08 alpha）
- 粒子瞬间"凝滞"（`uFlowSpeed` 降到接近 0，持续 1.5s 后恢复）
- 或：一个环形冲击波从画面中心向外扫过，扫过之处颜色改变（类似"时间涟漪"）

这会让观众明确感知到"作品换幕了"，强化"时辰"的仪式感。

### 3. 雨滴视觉升级
**现状**：`rain.frag` 是简单的竖条，颜色跟随 `core` 色。  
**建议**：
- 根据章节改变雨的"性格"：Storm 时雨倾斜角度加大、速度加快；Tide 时雨几乎垂直、缓慢
- 加入"风吹雨线"的弯曲感（用噪声给 x 偏移加二次项）
- 在午时/卯时等高 energy 章节，雨滴偶尔闪烁（模拟阳光穿透水滴）

### 4. 数据层的可视化（而非纯文字）
**现状**：数据叙事 `dataNote` 只在标题卡中以文字出现。  
**建议**：在画面中增加轻量的"数据层"UI——
- 巳时（NOAA GFS）：在画面边缘显示一个极淡的风向玫瑰图或风速数字（opacity 0.08，不干扰主体）
- 未时（GBIF）：显示一个极淡的洄游路径轮廓
- 辰时/酉时（通勤高峰）：在背景中显示极淡的城市路网热力图

这些"数据水印"让作品从"诗意模拟"真正走向"数据艺术"，也为产品化时的企业定制预留空间。

### 5. 交互反馈的"数据感"
**现状**：鼠标扰动是自然的流体力学效果，点击是同心圆涟漪。  
**建议**：
- 鼠标轨迹留下一条极淡的"数据尾迹"（渐隐的白色/章节色细线，0.3s 内消失）
- 点击涟漪改为"六边形网格波"或"二进制扩散"，呼应"数据主义"主题
- 现场声音触发时，在画面边缘显示极淡的频谱条（opacity 0.06），让观众"看见"声音

### 6. 加载屏幕的艺术化
**现状**：`LoadingScreen` 是纯黑 + 文字。  
**建议**：加载时显示一个"粒子正在凝聚"的动画——从 500 个随机点逐渐收敛到椭圆带的形状，加载完成时"爆开"进入主画面。这比静态文字更符合作品的气质。

---

## 四、产品逻辑改进建议

### 1. "Dataism" 名的实相符
**现状**：项目名为 Dataism（数据主义），但只有 DroneStorm 接入了真实数据，十二时辰中的 巳/未 只是"预留接口"。  
**建议**：这是概念完整度的核心缺口。至少完成以下数据接入：
- **巳时 NOAA GFS**：把 DroneStorm 的风场渲染模式作为巳时的"数据层"，粒子运动不再只是程序化噪声，而是真实风场的 u/v 分量驱动
- **未时 GBIF**：完成 SalmonRun 的粒子沿洄游路径约束运动，哪怕只有 64 条观测记录
- **子时潮汐**：接入潮汐 API（如 WorldTides），让粒子流动速度与真实潮汐流速联动
- **午时太阳高度**：接入 sun position API，让画面亮度与真实太阳高度角联动（不仅仅是固定 energy 值）

**一句话**：作品叫 Dataism，观众应该能明确指出"哪些东西是真实数据"。

### 2. 确定性渲染的"鉴赏界面"
**现状**："确定性即可鉴定"是护城河声明，但普通观众无法感知。  
**建议**：增加一个隐蔽的"鉴定模式"（如连按 Shift+D 3 次），显示：
- 当前种子号（mulberry32 种子）
- 当前 git tag 版本
- 当前帧的粒子分布哈希（快速校验和）
- 最后一次数据更新时间

这既是防伪技术，也是收藏家津津乐道的"作品身份证明"。

### 3. 移动端不是完全放弃
**现状**：移动端直接显示 fallback 文案。  
**建议**：至少提供一个"触摸版"——
- 粒子数降到 8000
- 触摸拖动代替鼠标
- 点击产生涟漪
- 不加载音频（或提供简化版合成音）

这会让朋友圈分享、小红书传播成为可能。目前的"请用桌面"等于主动放弃流量入口。

### 4. 展厅心跳系统（L3 刚需）
**现状**：PRODUCTIZATION.md 中列出但未实现。  
**建议**：最简单的实现：
```js
// 每天一次 POST 到自托管端点
setInterval(() => {
  fetch('https://heartbeat.dataism.studio/ping', {
    method: 'POST',
    body: JSON.stringify({
      deviceId: localStorage.getItem('deviceId'), // 首次生成
      version: __GIT_TAG__, // Vite define 注入
      uptime: performance.now(),
      fps: window.__lastFps,
      chapter: currentChapter.branch,
      micStatus,
    })
  })
}, 24 * 60 * 60 * 1000)
```
配合一个简单的 Grafana dashboard，运营方就能远程监控所有展厅状态。

### 5. 实体装置的"开箱体验"
**现状**：没有 Tauri 封装，没有离线运行方案。  
**建议**：
- Tauri 封装优先级其实可以排后——直接用 Chrome/Chromium 的 `--kiosk` 模式启动 + 本地 nginx 托管 dist 更轻量
- 真正的刚需是**一键安装脚本**：一条命令完成 kiosk 启动、开机自启、崩溃自动重启（systemd 或 launchd）
- 配套一个"首次激活"流程：扫码输入展厅编号 → 自动注册到心跳系统 → 拉取配置（亮度上限、静音时段等）

### 6. 企业定制的模板化
**现状**：企业定制在 PRODUCTIZATION.md 中是 P1.5，但引擎层面没有预留接口。  
**建议**：
- 在 `chapters.js` 之上抽象一个 `ChapterTemplate` 系统：品牌色 → 自动插值生成 12 时辰色板
- 数据接口槽位标准化：`{ id: 'noaa', fetcher, renderer }` 的插件式注册
- 提供企业版的 `?brand=xxx` 查询参数，自动替换标题卡 logo 和铭牌文案

---

## 五、实施路线图建议

| 阶段 | 时间 | 目标 | 关键产出 |
|------|------|------|----------|
| **v1.2 展厅加固** | 2 周 | 上下文恢复 + 性能分级 + 噪声门 | 敢出货的首台实体装置 |
| **v1.3 数据觉醒** | 3 周 | 巳时 NOAA + 未时 GBIF + 午时太阳高度 | "Dataism" 名副其实 |
| **v1.4 审美跃迁** | 2 周 | Bloom 后处理 + 粒子形状升级 + 转场仪式 | 展厅级的视觉震撼 |
| **v1.5 产品化** | 3 周 | 心跳系统 + 一键安装脚本 + 移动端轻量版 | 可规模化部署的商品 |

---

## 六、结语

Dataism 已经是一个**概念先行、架构扎实、审美克制**的优秀项目。十二时辰的叙事框架和"确定性即可鉴定"的产品化思路，在数字艺术领域具有真正的差异化。

当前最需要补齐的，按优先级排序：

1. **技术层**：WebGL 上下文恢复 + 性能分级（决定"能不能 7×24 跑"）
2. **概念层**：真实数据接入（决定"Dataism 是否名副其实"）
3. **审美层**：Bloom + 转场仪式 + 粒子形状变化（决定"展厅够不够震撼"）
4. **产品层**：心跳监控 + 移动端轻量版 + 企业模板（决定"能不能规模化"）

建议先集中精力做 v1.2（展厅加固），这是产品化的生死线。数据层和审美层可以并行推进。
